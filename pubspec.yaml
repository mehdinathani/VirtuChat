name: virtuchat
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  cloud_firestore: ^4.14.0
  firebase_auth: ^4.16.0
  firebase_core: ^2.24.2
  flutter:
    sdk: flutter
  google_gemini: ^0.1.2
  google_sign_in: ^6.2.1
  stacked: ^3.4.0
  stacked_services: ^1.1.0
  file_picker: ^6.1.1
  firebase_storage: ^11.6.0
  image_picker: ^1.0.7
  share_plus: ^7.2.1


dev_dependencies:
  build_runner: ^2.4.5
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mockito: ^5.4.1
  stacked_generator: ^1.3.3

flutter:
  uses-material-design: true


 # Add the following section to specify the assets directory
  assets:
    - assets/image/
